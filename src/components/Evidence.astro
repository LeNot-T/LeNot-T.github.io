---
const evidenceCategories = [
  {
    id: "modelado",
    label: "Modelado",
    color: "var(--ctp-lavender)",
  },
  {
    id: "normalizacion",
    label: "Normalización",
    color: "var(--ctp-peach)",
  },
  {
    id: "sql",
    label: "SQL",
    color: "var(--ctp-mauve)",
  },
  {
    id: "project",
    label: "Proyecto",
    color: "var(--ctp-pink)",
  },
];
---

<section class="evidence">
  <div class="content">
    <h2 class="section-title">Evidencias</h2>
    <div class="evidence-list">
      {
        evidenceCategories.map((category) => (
          <a
          href={`#${category.id}`}
          class="evidence-link"
          style={`--evidence-color: ${category.color}`}
          >
          <span class="evidence-icon">◈</span>
          <span class="evidence-name">{category.label}</span>
          </a>
        ))
      }
    </div>
  </div>
</section>

<style>
  .evidence {
    padding: 6rem 2rem;
    display: flex;
    justify-content: center;
  }

  .content {
    max-width: 600px;
    width: 100%;
  }

  .section-title {
    font-size: 2rem;
    font-weight: 600;
    color: var(--ctp-text);
    margin-bottom: 3rem;
    text-align: center;
  }

  .evidence-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .evidence-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 2rem;
    background-color: var(--ctp-surface0);
    border: 2px solid var(--evidence-color);
    border-radius: 12px;
    text-decoration: none;
    color: var(--ctp-text);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .evidence-link::before {
    content: "";
    position: absolute;
    inset: -1px;
    background-color: var(--evidence-color);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 0;
  }

  .evidence-link:hover::before {
    transform: scaleX(1);
  }

  .evidence-icon {
    font-size: 1.5rem;
    color: var(--evidence-color);
    transition: all 0.3s ease;
    z-index: 1;
  }

  .evidence-name {
    flex: 1;
    font-size: 1.25rem;
    font-weight: 500;
    z-index: 1;
    transition: color 0.3s ease;
  }

  .evidence-arrow {
    width: 24px;
    height: 24px;
    color: var(--evidence-color);
    transition: all 0.3s ease;
    z-index: 1;
  }

  .evidence-link:hover {
    transform: translateX(8px);
  }

  .evidence-link:hover .evidence-icon,
  .evidence-link:hover .evidence-arrow {
    color: var(--ctp-base);
  }

  .evidence-link:hover .evidence-name {
    color: var(--ctp-base);
  }

  @media (max-width: 600px) {
    .evidence {
      padding: 4rem 2rem;
    }

    .section-title {
      font-size: 1.5rem;
      margin-bottom: 2rem;
    }

    .evidence-link {
      padding: 1.25rem 1.5rem;
    }

    .evidence-name {
      font-size: 1.1rem;
    }
  }
</style>
